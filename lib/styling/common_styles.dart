// styles.dart

import 'package:flutter/material.dart';

class CommonColors {
  static const Color appBarBackground = Color(0xFF3F3D40);
  static const Color appBarIcon = Colors.white;
  static const Color validationIcon = Colors.white;
}

class CommonStyles {
  static const TextStyle appBarTitle = TextStyle(color: Colors.white);
  static const TextStyle validationText = TextStyle(color: Colors.white, fontSize: 40);
  static const TextStyle validationRetryText = TextStyle(color: Colors.white, fontSize: 20);
  static const TextStyle scannerText = TextStyle(color: Colors.white, fontSize: 10);
}